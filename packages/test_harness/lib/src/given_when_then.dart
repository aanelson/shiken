class Given<T> {
  const Given(this.harness);

  final T harness;
}

class When<T> {
  const When(this.harness);

  final T harness;
}

class Then<T> {
  const Then(this.harness);

  final T harness;
}
